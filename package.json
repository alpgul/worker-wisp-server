{
  "name": "wisp-worker",
  "version": "0.8.7",
  "description": "A Wisp v1/v2 server implemented as a Cloudflare Worker. TCP is relayed over Cloudflare's connect() API and works on all plans, including free.",
  "license": "AGPL-3.0-or-later",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node --test --experimental-loader ./test/imports-loader.mjs \"test/*.test.js\""
  },
  "devDependencies": {
    "wrangler": "^4.129.0"
  }
}